import {themeSpacing} from '../constants';

export default {
    styleOverrides: {

        root: {
            paddingBottom: themeSpacing[2] + "px",
            paddingTop: themeSpacing[2] + "px",
            fontWeight:500,
        }
    }
}
